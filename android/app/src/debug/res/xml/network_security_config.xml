<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG BUILDS ONLY — this file lives under src/debug and is not merged into a
  release APK.

  The Firebase Auth emulator is served over plain HTTP on the host machine
  (10.0.2.2 is how the Android emulator reaches it). Android blocks cleartext
  by default, so `verifyPhoneNumber` fails instantly with an opaque
  "An internal error has occurred" — the SmsRetrieverHelper's real message is
  "Cleartext HTTP traffic to 10.0.2.2 not permitted".

  Scoped to the loopback host alone: every other destination, including all
  production traffic, still requires TLS even in a debug build.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
